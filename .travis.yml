sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t dadsweb/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dadsweb/docker-react npm run test -- --coverage
